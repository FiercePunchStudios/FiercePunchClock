# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter:  mysql
  hostname: localhost
  port:     3306
  database: localdb
  username: localmysqluser
  password: yourpassword
  #socket:   
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlite3
  database: db/test.sqlite3
  pool: 5
  timeout: 5000

development:
  adapter:  mysql
  hostname: localhost
  port:     3306
  database: sharedaccountname_dbname
  username: sharedaccountname_username
  password: hostgeneratedpassword
  #socket:   
  pool: 5
  timeout: 5000
